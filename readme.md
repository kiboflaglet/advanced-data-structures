Each data structure can be found inside src folder with example demonstrations along with readme file

inside index.ts I imported every file to run but i commented out them to execute them seperately (fast, not overthinking)

just run ```yarn dev``` to see the results